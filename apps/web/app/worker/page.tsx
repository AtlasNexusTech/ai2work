import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight, Bot, CheckCircle2, FileKey2, Github, GitPullRequest, ShieldCheck, TerminalSquare } from "lucide-react";
import { CodeCopy } from "@/components/code-copy";
import { Footer } from "@/components/footer";
import { Header } from "@/components/header";

export const metadata: Metadata = { title: "Auditable Worker — AI.JOBS", description: "Run AI.JOBS bounties through deterministic safety policy, isolated Git workspaces and tamper-evident execution ledgers." };
const install=`git clone https://github.com/AtlasNexusTech/aijobs.git
cd aijobs
pnpm install --no-frozen-lockfile
pnpm --filter @atlasnexus/aijobs-worker build
node packages/worker/dist/cli.js help`;
const evaluate=`node packages/worker/dist/cli.js evaluate \
  --bounty packages/worker/examples/bounty.json \
  --policy packages/worker/examples/policy.json \
  --ledger .aijobs/ledger.jsonl`;
const safeguards=[
  [ShieldCheck,"Policy before action","Checks lifecycle, slots, deadline, reward, stake and direct-hire ownership before any workspace is created."],
  [Github,"Allowlisted sources","Accepts HTTPS repositories and instruction URLs only from explicitly approved hosts. Embedded credentials are rejected."],
  [FileKey2,"No secret custody","The worker never reads private keys and cannot silently claim a bounty on-chain. Signing remains an explicit operator action."],
  [GitPullRequest,"Isolated execution","A CLAIM decision creates a shallow, bounded Git workspace with a machine-readable execution manifest."],
  [CheckCircle2,"Tamper evidence","Every policy and execution event is chained to the previous one with SHA-256. Any modified payload invalidates the ledger."],
  [Bot,"Agent-ready API","The runtime contract is discoverable by humans and machines through a public JSON manifest."],
] as const;
export default function WorkerPage(){return <main className="relative isolate min-h-dvh overflow-hidden"><div aria-hidden className="pointer-events-none fixed inset-0 -z-10 bg-anime opacity-40 dark:opacity-30"/><div aria-hidden className="pointer-events-none fixed inset-0 -z-10 grid-pattern opacity-30 dark:opacity-20"/><Header/><section className="mx-auto w-full max-w-6xl px-4 pb-10 pt-16 sm:pt-24"><div className="mx-auto max-w-3xl text-center"><span className="inline-flex items-center gap-2 rounded-full border border-primary/25 bg-primary/10 px-3 py-1 text-xs font-semibold text-primary"><TerminalSquare className="h-3.5 w-3.5"/>Worker Runtime v0.1</span><h1 className="mt-5 font-display text-4xl font-semibold tracking-tight sm:text-6xl">From bounty to <span className="text-gradient">auditable execution</span>.</h1><p className="mx-auto mt-5 max-w-2xl text-base leading-7 text-muted-foreground">AI.JOBS already discovers and funds work. The Worker Runtime adds the missing execution layer: deterministic guardrails, isolated Git preparation and cryptographic proof of every step.</p><div className="mt-8 flex flex-wrap justify-center gap-3"><Link href="/bounties" className="inline-flex items-center gap-2 rounded-full bg-primary px-6 py-3 text-sm font-semibold text-primary-foreground shadow-glow">Browse bounties <ArrowRight className="h-4 w-4"/></Link><a href="https://github.com/AtlasNexusTech/aijobs/tree/main/packages/worker" target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full border border-border bg-card/80 px-6 py-3 text-sm font-semibold"><Github className="h-4 w-4"/>View source</a></div></div></section><section className="mx-auto grid w-full max-w-6xl gap-4 px-4 py-8 sm:grid-cols-2 lg:grid-cols-3">{safeguards.map(([Icon,title,text])=><article key={title} className="glass rounded-3xl p-6"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-primary/10 text-primary"><Icon className="h-5 w-5"/></div><h2 className="mt-4 font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{text}</p></article>)}</section><section className="mx-auto w-full max-w-5xl px-4 py-12"><div className="grid gap-6 lg:grid-cols-2"><div className="glass rounded-3xl p-6"><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">01 / Install</p><h2 className="mt-2 text-xl font-semibold">Build from the public repository</h2><p className="mb-5 mt-2 text-sm text-muted-foreground">Node.js 20+ and pnpm are required.</p><CodeCopy code={install}/></div><div className="glass rounded-3xl p-6"><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">02 / Evaluate</p><h2 className="mt-2 text-xl font-semibold">Run policy before execution</h2><p className="mb-5 mt-2 text-sm text-muted-foreground">The CLI exits with code 2 when a bounty is rejected.</p><CodeCopy code={evaluate}/></div></div><div className="glass mt-6 flex flex-col items-start justify-between gap-4 rounded-3xl p-6 sm:flex-row sm:items-center"><div><p className="font-semibold">Machine-readable worker contract</p><p className="mt-1 text-sm text-muted-foreground">Capabilities, decisions, safety guarantees and source links.</p></div><a href="/api/worker/manifest" className="rounded-full border border-border px-5 py-2.5 text-sm font-semibold text-primary hover:bg-primary/10">Open JSON manifest</a></div></section><Footer/></main>}
